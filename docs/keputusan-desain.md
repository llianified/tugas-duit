# Keputusan desain yang sering disalahsangka bug

Daftar hal yang tampak seperti cacat saat dibaca sepintas, padahal memang
disengaja. Tujuannya satu: audit berikutnya tidak perlu menemukan ulang alasan
yang sama. Setiap baris menyebut tempat aturannya ditegakkan, supaya klaim di
sini bisa dicek, bukan dipercaya.

## Ekonomi

| Yang terlihat janggal | Kenapa memang begitu | Ditegakkan di |
| --- | --- | --- |
| Komisi referral yang melewati plafon harian **hangus**, tidak ditunda ke besok | Plafonnya plafon keras — batas *penerimaan* per hari, bukan antrean. Menundanya akan menaikkan total payout jangka panjang. | `consumeCommissionQuota`; test `ECON-2 … menghanguskan sisanya` |
| Komisi referral tidak ikut menghabiskan plafon task | Komisi sudah didanai task downline yang masing-masing sudah dibatasi plafonnya sendiri. Kolom `commission_credits` sengaja terpisah dari `credits_earned`. | migrasi `0011` |
| `withdrawals.amount_idr` bisa tidak sama dengan `credits × kurs sekarang` | Kurs dibekukan saat pengajuan. Constraint `amount_idr = credits * 100` sengaja dilepas supaya kurs boleh berubah tanpa mengubah pengajuan lama. Semua tampilan rupiah penarikan membaca kolom ini, bukan menghitung ulang. | migrasi `0013`; `server/messaging/notify.ts`, `withdraw-receipt.tsx` |
| `daily_quotas.tasks_completed` dinaikkan dulu, lalu **dikembalikan** kalau task-nya tidak dibayar | Penghitungnya dinaikkan lebih dulu supaya kenaikannya ikut terkunci baris `daily_quotas` yang sama, bukan karena penolakan ikut dihitung. Tanpa pengembaliannya jaring anti-bot menghitung percobaan alih-alih task, dan user yang sekali menabrak plafon tidak akan pernah turun lagi dari plafon itu di hari yang sama. | `consumeQuota`; test `AUDIT-1` |
| Penghasilan task ditahan kolam yang mengisi ulang bertahap, **bukan** plafon yang reset tengah malam | Reset harian memusatkan seluruh permintaan di satu jam dan menghukum user yang zona waktunya berbeda. Kolam memberi alasan untuk kembali tanpa tenggat. Bentuknya sengaja dikembarkan dengan energi. | `domain/economy/reward-pool.ts`; migrasi `0023` |
| Reward yang lebih besar dari sisa kolam **dipotong**, bukan ditolak | Task yang sudah dijawab benar tetap dibayar sebagian; menolaknya menghanguskan usaha user hanya karena kolamnya kurang satu credit. | `applyRewardPoolSpend`; test `pays only what is left instead of refusing the last task` |
| Bonus rank dan streak menambah kapasitas kolam, bukan laju isi ulangnya | Kapasitas mengubah berapa yang bisa ditumpuk lalu dihabiskan dalam satu sesi, tanpa menaikkan penghasilan maksimum per hari. Menaikkan laju menggeser seluruh biaya jangka panjang. | `rewardPoolCapacity`; `domain/economy/reward-pool.test.ts` |
| Menit yang belum genap satu interval tidak hangus saat kolam dibelanjakan | Jam acuan hanya dimajukan sebanyak interval yang benar-benar dibayar. Tanpa ini, user yang membuka app tepat sebelum interval genap kehilangan progres isi ulangnya setiap kali. | `regenAnchor` di `domain/economy/reward-pool.ts` |
| Iklan memberi **tiket masuk task**, bukan energi atau credit | Hadiah energi hangus tanpa jejak saat energi penuh (`applyEnergyGrant` memotong di `maxEnergy()`) dan menggeser jangkar regen sehingga hitung mundur di UI melompat. Tiket disimpan sebagai barisnya sendiri, jadi tidak pernah hangus diam-diam dan niatnya terbaca di data. Yang lebih penting: tiket tidak mencetak Rupiah — bayarannya tetap lewat kolam reward dan `daily_quotas`, jadi klaim palsu hanya mempercepat user menghabiskan plafonnya sendiri. | migrasi `0024`; `server/ads/ads.ts` |
| Klaim iklan dari klien **tidak lagi** dipercaya sendirian, tapi hanya kalau gerbangnya dinyalakan | Alasan lama sudah kedaluwarsa: verifikasi server-ke-server dulu terkunci di ambang DAU Adsgram, sedangkan Monetag membukanya untuk semua akun. Postback menutup dua hal sekaligus yang tidak bisa ditutup dari klien — menekan iklan lalu menekan back (SDK tetap resolve) dan memanggil `/api/ads/claim` langsung tanpa satu pun iklan tayang. Yang menentukan sah cuma satu hal: Monetag membayar event itu atau tidak. Gerbangnya tetap sebuah saklar panel (`adsPostbackRequired`) dan bawaannya mati, karena menyalakannya sebelum URL postback terisi di dashboard membuat SELURUH tiket berhenti terbit — dan gejalanya cuma tombol iklan yang tidak pernah berhasil. Saat gerbangnya mati, pengamannya tetap yang lama: plafon harian, cooldown, dua indeks unik parsial, dan sinyal fraud. | ADR `0004`; `server/ads/postback.ts`; migrasi `0041`; test `ADS-DB-12` |
| `claimAdTicket` menjawab **sukses** untuk baris yang sudah `ready`, padahal klaim itu tidak menerbitkan apa pun | Sejak `settleAdPostback` ada, ada DUA penerbit pass — dan yang kedua jalan tanpa memeriksa `adsPostbackRequired`, jadi ia sudah aktif selama masa pembuktian yang gerbangnya masih mati. Postback dan klaim klien berangkat dari momen yang sama, jadi siapa yang menang murni balapan. Membaca baris `ready` milik user itu sendiri sebagai tiket karangan berarti user membaca "tiket iklan tidak ketemu" tepat setelah menonton iklan penuh — sementara passnya justru sudah siap — dan namanya ikut ditulis ke `fraud_signals`, yang lalu menaikkan skor risiko yang dibaca admin tepat sebelum mentransfer uang. Baris `consumed`/`expired` juga bukan karangan, jadi tidak menerbitkan sinyal. Klasifikasinya sengaja dikembarkan persis dengan `readVerifiedClaim`. | `claimAdTicket`; test `ADS-DB-14` |
| Jalur postback ikut menyapu pass yang sudah lewat tenggat sebelum menerbitkan | Slot `ad_views_one_ready` cuma disapu di `readState`, dan `restoreAdPass` bisa menghidupkan pass memakai tenggat ASLINYA — jadi pass yang tinggal semenit bisa mati justru saat sebuah tiket sedang menunggu konfirmasi. Tanpa sapuan ini promosinya berhenti di `noted`, route menjawab 200, dan Monetag berhenti mengulang: impresinya terbayar, `verified_at` tertulis, tapi tiketnya tidak pernah terbit — dan saat gerbangnya menyala tidak ada jalur lain yang bisa menyusul. Cacat yang sama pernah ditanggung `restoreAdPass`. | `settleAdPostback`, `restoreAdPass`; test `ADS-DB-13`, `ADS-DB-14` |
| Lama tontonan minimum ditegakkan pada klaim tiket rewarded, bukan pada postback atau interstitial in-app | Tiket rewarded memerlukan dua bukti saat gerbang postback aktif: Promise SDK dari klien harus tiba setelah `adsMinWatchSeconds`, dan Monetag harus mengonfirmasi event berbayar. Postback boleh datang lebih dulu, tetapi hanya mencatat `verified_at` dan menunggu klaim klien. Interstitial otomatis `type: 'inApp'` berada di jalur berbeda: ia tidak membuka `ad_views`, tidak mengirim `ymid`, tidak memanggil endpoint klaim, dan tidak memberi pass, sehingga tidak mempunyai minimum watch aplikasi. | `adWatchTooShort` di `claimAdTicket`; `inAppShowParams`; test `ADS-DB-15`, `INAPP-1` |
| `adsMinWatchSeconds` lahir 3 detik, dan angka itu memang belum cukup menutup "tap iklan lalu back" | 3 detik adalah lantai lama `MIN_WATCH_MS` apa adanya, yang selama ini hanya MENCATAT sinyal lalu tetap menerbitkan tiket. Dibawa jadi bawaan supaya menyalakan penegakannya tidak menghanguskan satu pun tiket rewarded yang selama ini sah — deploy-nya netral. Angka yang benar-benar memblokir hampir pasti lebih tinggi, tapi ia tidak boleh ditebak: jendelanya ikut memuat waktu memuat SDK (`waitForShow`, sampai 8 detik), jadi ia tidak sama dengan durasi kreatifnya. Pilih dari sebaran `ready_at - created_at` yang sudah tercatat, setel sedikit di bawah persentil 5. Terlalu tinggi = tiket rewarded berhenti terbit sementara impresinya tetap dibayar penyedia. Angka ini tidak memengaruhi in-app. | `adsMinWatchSeconds` di `ECONOMY_FIELDS`; migrasi `0042` |
| `adsMaxViewsPerDay = 0` mematikan lebih dari sekadar tombol iklannya | Ia tombol mati seluruh fitur iklan, dan dua fitur lain berdiri di atasnya. Misi harian "Tonton N iklan" berhenti diterbitkan, karena misi yang mustahil lebih buruk daripada misi yang hilang: progresnya berhenti di 0/N selamanya, dan `hasUnclaimedMissions` membuat titik pengingat di nav menyala permanen tanpa satu pun cara membersihkannya. Arena yang dikunci iklan ikut tertutup — alasannya di bagian Arena. Tombol matinya sengaja tetap SATU field: yang menyesuaikan efeknya, bukan admin yang harus menyetel tiga field bersamaan. Validator hanya menangkap `missionAdsTarget > adsMaxViewsPerDay` saat plafonnya di atas nol, yaitu satu-satunya keadaan di mana misinya terbit tapi tetap mustahil. | `missions()`; `validateEconomyConfig`; migrasi `0040`; test `misi iklan mengikuti tombol mati iklan` |
| `challenges.ad_view_id` **tidak** dikosongkan saat tiketnya dikembalikan | Tiket yang dihidupkan ulang harus boleh membayar challenge berikutnya, dan itu diurus indeks `challenges_ad_view_unique` yang disempitkan ke `energy_refunded_at is null` — bukan dengan menghapus jejaknya. Mengosongkan kolomnya akan melepas satu-satunya bukti bahwa challenge itu pernah membayar ongkos masuk, dan constraint `challenges_entry_refund_needs_entry` kehilangan pijakannya. | migrasi `0024`; test `ADS-DB-3` |
| Interstitial otomatis menitipkan **jadwalnya ke SDK** lewat `show_<zone>({ type: 'inApp', inAppSettings })`, bukan menembak satu per satu dari timer aplikasi | Tanpa discriminator `type: 'inApp'`, Monetag membaca `show_<zone>()` sebagai Rewarded Interstitial — formatnya berubah, bukan cuma penjadwalannya. Jadi payload native inilah satu-satunya cara meminta in-app, dan konsekuensinya diterima: jadwalnya milik SDK dan **tidak punya API untuk dibatalkan, dijeda, atau diubah**. Menetralkannya lewat `capping: 0, interval: 0` sudah dicoba dan justru menghapus seluruh plafon (jendela nol jam tidak membatasi apa pun). Yang tinggal di aplikasi hanya angkanya — `domain/ads/in-app-ads.ts` menyusun parameter dan menaikkan jendela ke lantai `minInAppWindowSeconds()` — plus penjaga satu pendaftaran per dokumen (`initializedZones`), karena tiap panggilan payload ini meninggalkan satu penjadwal abadi. | `shell/use-in-app-ads.ts`; `domain/ads/in-app-ads.ts`; test `INAPP-1` |
| Interstitial masih bisa jatuh di tengah task, dan aplikasi **tidak berpura-pura bisa mencegahnya** | Bayaran task dihitung dari waktu (`getStars` atas `started_at`), jadi iklan yang menutup layar di tengah captcha memotong bintang — pada soal Mudah, jendela 3★ cuma 10 detik. Perbaikannya terbatas karena SDK tidak menyediakan pause/cancel: satu-satunya kendali yang tersisa adalah **kapan jadwalnya didaftarkan**. `useInAppAds` karena itu menahan pendaftaran selama ada task berjalan, yang menghilangkan tabrakan tayangan PERTAMA (bawaan `timeoutSeconds` 5 detik, persis saat user menekan Mulai) tanpa membatalkan apa pun, tanpa mengubah format, dan tanpa mengurangi impresi — hanya menggesernya. Tayangan berikutnya tetap di luar kendali aplikasi. Menutup sisanya butuh jadwal milik aplikasi sendiri, dan itu memaksa `show_<zone>()` polos yang mengubah format iklan; jangan menempuhnya diam-diam. Lever yang aman berikutnya ada di panel ekonomi (`inAppAdsTimeoutSeconds`, `inAppAdsIntervalSeconds`), bukan di kode. | `shell/use-in-app-ads.ts`; `shell/app-shell.tsx` |
| Tombol iklan tetap muncul walau energi masih penuh | Kartu task menawarkan dua cara bayar berdampingan, dan klien mengirim `payWith` secara eksplisit — server tidak pernah diam-diam memakai tiket saat user menekan tombol energi. Pass yang belum dipakai hangus lewat `adsPassTtlMinutes`, bukan diam-diam terpakai. | `startChallenge`; `features/home/active-task.tsx` |
| Tiket iklan tidak boleh dibuka selama task **atau ronde Arena** yang dibayarinya belum ditutup | Pass yang sudah dipakai baru bisa dihidupkan lagi kalau slot `ad_views_one_ready` kosong. Tanpa penjaga ini, user yang menonton iklan baru selagi task berbayar-tiket masih jalan membuat tiket lamanya tidak bisa dikembalikan saat task itu hangus — tiketnya hilang tanpa jejak. Menolak di depan lebih murah daripada menambah stok pass. Arena memotong pass lewat `consumeAdPass` yang sama, jadi ronde yang terbuka ikut dihitung — dengan saringan umur `ARCADE_OPEN_PLAY_TTL_MINUTES`, karena sapuan ronde basi hanya jalan saat Arena dibuka dan tanpa saringan itu satu ronde yang ditinggal akan mengunci tiket user selamanya. | `adOpenRefusal` (`entry_open`); `STATE_SQL` di `server/ads/ads.ts`; test `ADS-DB-7`, `ADS-DB-11` |
| Membuka tiket dua kali menyerahkan tiket yang **sama**, bukan menolak | Tiket adalah izin menonton satu iklan, bukan hasil tontonan. Kalau SDK Adsgram gagal dimuat atau tayangannya batal, izin itu masih berlaku sampai `adsTicketTtlSeconds` habis; menolak permintaan berikutnya hanya mengunci user 5 menit tanpa menambah pengaman apa pun. Barisnya tetap satu, jadi `ad_views_one_pending` tidak berubah artinya. | `openAdTicket`; test `ADS-DB-8` |
| Premium mematikan **interstitial otomatis saja**, tiket berhadiah tetap hidup | Alasannya jujur soal pemasukan: iklan berhadiah adalah impresi yang membayari reward pool, dan mematikannya untuk pembeli premium menghapus pendapatan tanpa menghapus keluhan — yang mengganggu itu iklan yang nongol sendiri, bukan tombol yang mereka tekan sendiri. Karena itu `ads.enabled` (tiket, opt-in) dipisah dari `ads.inAppEnabled` (otomatis), dan larangan premium dicabut dari `openAdTicket` supaya server tidak menolak tombol yang klien masih render. Konsekuensinya wajib dibayar di copy: judul manfaatnya tidak boleh berbunyi "bebas iklan", dan daftar manfaat menyebut sendiri bahwa premium bukan nol iklan. | `readAdsState`; `features/premium/benefits.ts`; test di `server/premium/premium.test.ts` |
| Jatah tayangan harian dihitung dari tiket yang **diklaim**, bukan yang dibuka | Plafonnya plafon hadiah. Tiket yang iklannya tidak pernah selesai tayang tidak memberi apa pun, jadi memotongnya dari jatah menghukum user atas kegagalan jaringan. Yang menahan pembukaan tiket beruntun tetap `ad_views_one_pending` plus cooldown. | `STATE_SQL` (`ready_at is not null`); test `ADS-DB-8` |
| Reward dijepit ke `challenges.max_reward`, bukan menolak pembayaran | `max_reward` adalah plafon yang dijanjikan ke user saat soalnya terbit. Kalau admin menaikkan reward selagi soal itu beredar, membayar angka baru berarti membayar di atas yang ditampilkan; melempar error berarti soalnya macet dan user terkunci sampai kedaluwarsa. Dijepit membayar tepat sebesar janjinya. | `submitAnswer`; test `ECON-5` |
| Batas task harian dan kolam kosong adalah dua penolakan berbeda | Keduanya sama-sama mengembalikan ongkos masuk, tapi obatnya berbeda: kolam terisi sendiri dalam hitungan menit, plafon harian baru lepas besok. Satu pesan untuk dua sebab membuat user menunggu sesuatu yang tidak akan datang. | `consumeQuota` (`QuotaRefusal`); `app/api/task/submit/route.ts` |
| `referralCommissionPercent` tidak boleh 0, padahal 0% terdengar seperti "matikan komisi" | Pada 0% `commissionUnitsForReward` bernilai nol dan `accrueCommission` berhenti sebelum menulis baris. Syarat penarikan menghitung downline dari `referral_commissions`, jadi 0% mengunci penarikan setiap user yang belum terlanjur memenuhi syaratnya — diam-diam, tanpa pesan yang menjelaskan. Gerbang referralnya sendiri sekarang `withdrawalMinActiveReferrals` dan boleh diisi 0 — jadi yang menahan `min: 1` bukan lagi ketiadaan pasangan setelan, melainkan bahwa 0% mematikan komisi lewat jalur yang tidak terbaca sebagai setelan: barisnya tidak ditulis, dan pengaruhnya sampai ke syarat penarikan. Mematikan komisi yang sungguhan berarti mengisi gerbangnya 0, bukan persennya. | `min: 1` di `ECONOMY_FIELDS`; test `komisi nol mengunci syarat penarikan` |
| Manfaat premium pertama yang menambah **penghasilan**, bukan kapasitas | Semua manfaat premium sebelumnya berbentuk kapasitas atau kecepatan — energi, stok reward, batas soal harian, jeda penarikan — dan tidak satu pun menambah rupiah yang bisa dihasilkan; semuanya cuma mempercepat sampainya user ke plafon yang sama. Komisi referral premium (`premiumReferralCommissionPercent`) memberi premium alasan ekonomi, bukan sekadar alasan kenyamanan. Ia HARUS berpasangan dengan `premiumDailyCommissionCapIdr`: plafon harian adalah penjaga terakhir komisi, jadi menaikkan persennya sendirian tidak menambah apa pun bagi upline yang sudah mentok — manfaat yang tertulis di kartu tapi tidak pernah terasa. Yang menentukan laju adalah premium UPLINE, dibaca sekali bersama id-nya di `accrueCommission` supaya laju dan plafon berasal dari satu potret waktu. Liabilitas maksimumnya Rp12.000/hari per akun premium, di atas harga sebulan — jadi angkanya ditinjau bersama harga, bukan disetel sekali lalu ditinggal. | `accrueCommission`; `consumeCommissionQuota`; migrasi `0052`; test di `server/economy/referral.test.ts` |
| Emas premium sekarang punya **gerak**, dan geraknya lambat | Premium sudah punya warna dan bentuk; yang belum dimilikinya gerak. `.premium-sheen` menyapu perangko yang belum dibeli dengan jeda 7 detik — bukan 2,6 detik seperti `.cta-sheen`, karena kartu yang cuma lewat di beranda bukan tombol yang sedang ditunggu ditekan, dan pada jeda tombol ia berubah jadi kedipan yang menuntut perhatian terus-menerus. `.premium-glint` sengaja TIDAK dipasang di mahkota papan peringkat: animasi CSS pada elemen sejenis mulai serentak, jadi seluruh daftar akan berdenyut bersamaan. Ia hanya dipakai pada mahkota yang menyatakan status pembacanya sendiri, tempat ia satu-satunya di layar. Keduanya tunduk pada `prefers-reduced-motion`. | `.premium-sheen`, `.premium-glint` di `app/globals.css` |
| Satuan saldo berganti nama jadi **TD**, tapi hanya di lapisan tampilan | Kata "credit" tersebar di puluhan berkas, jadi penggantinya ditulis satu kali sebagai `TOKEN_SYMBOL` di `shared/lib/format.ts` dan lambangnya berdiri sendiri di `shared/components/token-mark.tsx`. Yang sengaja TIDAK ikut berganti: kolom DB (`balance_credits`, `credit_ledger`), key JSON API (`credits`), dan nama fungsi — mereka kontrak dan skema, dan menggantinya menuntut migrasi merusak plus deploy server-klien serentak demi sesuatu yang tidak pernah dilihat user. Karena itu membaca kode ini akan terus menemukan kata "credit"; yang menahannya agar tidak bocor ke layar adalah `COPY-1`, yang memasukkan kata itu ke daftar terlarang di permukaan peserta. Mark-nya monokrom `currentColor`, bukan dua warna brand: aplikasi ini gelap-saja, jadi bagian hitam logo aslinya hilang di latar dan birunya lenyap di atas bidang `--primary`. | `TOKEN_SYMBOL`; `shared/components/token-mark.tsx`; test `COPY-1` |
| Syarat penarikan dibuka **bertahap**: satu gerbang pada satu waktu, yang sudah lewat tetap bercentang | Baris ini pernah menyatakan kebalikannya — ketiganya tampil sejak layar pertama, supaya syarat berbayar tidak muncul sebagai tagihan setelah kerjanya selesai. Yang membatalkannya bukan alasan itu (ia masih berlaku) melainkan harganya di layar user baru: tiga syarat sekaligus di saat saldonya nol terbaca sebagai daftar yang mustahil, dan satu tugas pada satu waktu jauh lebih mungkin dikerjakan. Kompensasinya wajib dan bukan opsional: premium harus sudah pernah terbaca di tempat lain sebelum jadi baris terakhir di sini — kartu premium di beranda dan `premiumBenefitList` menyebut jeda penarikan, jadi gerbangnya bukan hal baru saat ia muncul. Pemotongannya di `withdrawalRequirements`, bukan di komponen: ia aturan urutan gerbang, sekelas `withdrawalGatingReason`, jadi tidak ada penyaji yang bisa membocorkan gerbang berikutnya. | `withdrawalRequirements`; `NotEligibleNote`; test `withdrawalRequirements` |
| Kelayakan penarikan dibaca satu fungsi (`readEligibility`), bukan dihitung ulang di tiap pemanggil | Dulu jalur baca dan jalur tulis punya SQL kembar. Saat premium menambah jeda 3 hari hanya jalur tulis yang ikut berubah, sehingga UI menahan pembeli premium sampai hari ketujuh padahal server sudah menerimanya sejak hari ketiga — mereka membayar untuk manfaat yang tidak bisa dipakai. Jeda dan syaratnya juga tidak boleh disalin ke teks UI: `cooldownDays` ikut dikirim supaya kalimatnya mengikuti aturan yang benar-benar berlaku untuk orang itu. | `server/payout/payout.ts`; test `WD-9`, `WD-10` |
| Setiap route yang memakai sesi wajib memanggil `checkRateLimit` | Bukan selera, melainkan aturan yang diuji: `GET /api/withdrawals` dan seluruh permukaan admin sempat lolos tanpa plafon, dan itu hanya terlihat saat membandingkan semua route sekaligus. | test `RL-2` |
| Laju polling di klien terikat plafon route-nya, dan perbandingannya diuji | Keduanya satu aturan yang ditulis di dua berkas, jadi selisihnya tidak pernah terlihat saat membaca salah satunya. Dialog QRIS premium memoll `/api/session` tiap 6 detik (600 permintaan/jam melawan plafon 100) sehingga jatah SELURUH app habis dalam sepuluh menit — untuk user yang justru baru membayar — dan umpan aktivitas memoll tiap 15 detik melawan plafon 120. Dua-duanya gagal diam: SWR menahan data lama sehingga tidak ada layar yang menyatakan ada yang salah. | test `RL-3`; `POLL_BUDGET` di `premium-dialog.tsx`, `ACTIVITY_POLL_MS` di `use-session-queries.ts` |
| Route yang menghitung dengan `economyConfig()` wajib memanggil `loadEconomyConfig()` lebih dulu | Melewatkannya tidak melempar — route-nya tetap jalan, hanya saja di instance yang dingin ia memakai `DEFAULT_ECONOMY_CONFIG` alih-alih yang tersimpan. `PATCH /api/admin/users/[publicId]` sempat begitu, jadi hibah energi dan isi ulang kolam dari panel dijepit di kapasitas bawaan sementara baris audit `admin_actions` ikut mencatat angka yang salah. Kesalahannya tidak deterministik, jadi tidak akan muncul di satu jalan uji manual. | test `ECON-6` |
| Gerbang penarikan berpindah dari **hari aktif** ke **premium** | Hari aktif dipilih karena umur akun tidak menuntut apa pun dan streak menuntut terlalu banyak — tujuh hari aktif menuntut task betulan di hari terpisah tanpa menghukum yang bolong. Yang akhirnya membatalkannya: pabrik akun memang cuma perlu MENUNGGU, dan menunggu berbiaya nol. Premium menuntut biaya nyata per akun, jadi ia penghalang yang jauh lebih mahal untuk diskalakan. Harganya dibayar user jujur, yang kehilangan gerbang waktu — sisa penahannya ambang saldo dan referral aktif. Bawaannya MATI (`withdrawalRequiresPremium` 0): menyalakan gerbang berbayar di atas saldo yang sudah dihasilkan adalah keputusan panel, bukan efek samping deploy. | `payoutRequiresPremium`; migrasi `0051`; test `WD-11` |
| Premium wajib aktif sampai penarikan **dibayar**, bukan hanya saat diajukan | Premium yang berakhir atau dicabut saat withdrawal masih `processing` membuat saldo ditahan untuk pengajuan yang tak lagi memenuhi syarat. Deploy dan maintenance mengembalikannya lewat ledger idempoten lalu menghapus record tanpa notifikasi. Penghapusan sengaja menghilangkan history, cooldown pengajuan itu, dan detail tujuan; pasangan `withdrawal_hold`/`withdrawal_refund` tetap append-only sebagai audit saldo. Settlement admin mengunci record yang sama dan mengulang pemeriksaan agar transfer dan refund tidak mungkin menang bersamaan. | `server/payout/payout-cleanup.ts`; migrasi `0060`; `settlePayout` |
| Angka ekonomi tidak ada di kode | Semuanya dari `domain/economy/economy-config.ts`, dipasang dari DB per request lewat `loadEconomyConfig()`. Menyetel ekonomi lewat panel admin, bukan deploy. | aturan keras #2 di `CLAUDE.md` |
| Gerbang wajib join channel **meloloskan** user yang keanggotaannya tidak bisa dipastikan | `readChannelMembership` mengembalikan `null` untuk tiga hal yang bukan salah user: Telegram down, token salah, dan bot yang bukan admin channel. Menutup gerbang di situ berarti satu gangguan di pihak kami mengunci seluruh basis user sekaligus. Yang menahan panggilan beruntun selama gangguan itu `outageUntil` di memori proses — peredam badai, bukan penyimpan keputusan. | `readChannelGateState`; test `CHAN-GATE … meloloskan user saat keanggotaan tidak bisa dipastikan` |
| Gerbang menutup app di klien, tapi di server hanya menjaga `POST /api/task/start` | Yang perlu ditegakkan di server adalah jalur yang **mencetak** credit, karena gerbang klien saja bisa dilewati dengan memanggil API langsung. Penarikan sengaja tidak ikut dijaga: memblokirnya menyandera saldo yang sudah terkumpul sebelum gerbangnya menyala, dan itu tidak menutup celah apa pun. Submit juga tidak dijaga — task yang ongkos masuknya sudah dibayar harus boleh diselesaikan. | `channelGateBlocks` di `app/api/task/start/route.ts` |
| Layar gerbang tetap menyediakan tombol **Tarik dana** | Alasan yang sama dengan baris di atas, dibawa sampai ke klien. Sebelumnya `AppShell` mengganti SELURUH pohon aplikasi dengan layar gerbang — termasuk dialog penarikannya — jadi janji "penarikan saldo tidak pernah ikut diblokir" di deskripsi `channelGateEnabled` tidak pernah benar-benar berlaku, dan menyalakan saklar itu mengunci saldo yang sudah terkumpul. Tombolnya muncul hanya kalau ada yang bisa ditarik atau ada pengajuan yang perlu ditengok. | `onWithdraw` di `features/channel/channel-gate.tsx`; dirangkai `shell/app-shell.tsx` |
| Hasil "bukan anggota" hanya disimpan semenit, hasil "anggota" berjam-jam | Umur cache-nya mengikuti siapa yang sedang menunggu. User yang baru saja join berdiri di depan layar gerbangnya dan menekan "Saya sudah join"; menahannya di cache lama berarti menyuruh orang yang sudah menurut untuk menunggu tanpa sebab. Sebaliknya orang jarang keluar channel, jadi hasil positif boleh awet. Tombolnya sendiri memaksa cek ulang lewat `force`, jadi cache negatif bukan satu-satunya jalan keluar. | `MEMBER_TTL_MS`/`NON_MEMBER_TTL_MS` di `server/integrations/channel.ts`; test `CHAN-GATE … memaksa cek ulang` |
| Penanda gerbang tidak menumpang `channel_bonus_claimed_at` | Kolom bonus tidak pernah kembali ke null, jadi user yang klaim bonus lalu keluar dari channel akan terbaca sebagai anggota selamanya. Gerbang menuntut jawaban yang bisa berubah, dan tiga kemungkinan — anggota, bukan anggota, belum pernah dicek — tidak muat di satu timestamp. | migrasi `0033` |
| `economyConfig()` adalah state global proses | Dipasang ulang di awal setiap request oleh `loadEconomyConfig()` (cache 30 detik), jadi konsisten di dalam satu request. Nilai turunannya harus dibaca lewat fungsi, tidak boleh dibekukan ke `const` tingkat modul. | `server/economy/economy-config.ts` |
| Payload captcha **selalu memuat jawabannya**, dan itu bukan cacat | `const { answer, ...payload } = generated` memang membuang kolom `answer`, tetapi ketiga tipe soal tetap mengirim bahan yang cukup untuk menurunkannya dalam satu baris kode: `display` tipe `text` ADALAH jawabannya, `expression` tipe `math` bisa dihitung, dan `options` tipe `select` membawa `key`-nya. Ini disengaja. Seluruh postur anti-otomasi bersandar pada kolam reward, plafon harian, dan sinyal fraud — bukan pada kesulitan soal — dan postur itu memang yang bekerja: skrip paling sempurna sekalipun tidak bisa melewati plafon kolam, jadi ia hanya menghabiskan jatahnya sendiri lebih cepat. Dicatat di sini supaya telaah berikutnya tidak menyandarkan keputusan pada perlindungan yang tidak pernah ada, hanya karena namanya "captcha". | `domain/task/challenge.ts`; kolam reward di `server/economy/reward-pool.ts` |
| Misi sosial membayar energi atas aksi yang **tidak pernah diverifikasi** | "Follow X", "like & repost", dan dua misi post hanya menuntut link pernah diketuk dan `SOCIAL_MISSION_COOLDOWN_MS` berlalu menurut jam Postgres. Tidak ada pemeriksaan ke platformnya, dan memang tidak akan ada: hadiahnya energi (bawaan 1), dua di antaranya sekali seumur akun, dan energi berbiaya nol karena kolam reward tetap mematok pengeluaran. Cap waktunya ditulis server lewat `startMissionAction`, jadi jam perangkat tidak bisa dikarang. Alasan yang sama dengan klaim iklan dan hasil ronde Arena. | `domain/progression/missions.ts`; `server/task/missions.ts` |
| Pemasukan tunai kedua masuk lewat tabel pesanan **sendiri**, bukan dengan melebarkan `premium_payments` | Tabel itu memegang uang yang sudah berjalan di produksi dan bentuknya premium sampai ke nama kolomnya (`months`, `granted_until`). Melebarkannya berarti mengendurkan `not null` dan check pada tabel yang sedang dipakai, tepat di jendela ketika kode lama masih hidup. `cash_orders` berdiri di sebelahnya dengan alur pelunasan yang bentuknya sama persis — dan kesamaan itu disengaja: setiap penjagaan di sana (tagihan lama ditanyakan dulu ke gateway, gateway bisu berarti "tidak tahu" bukan "tidak dibayar", signature membuktikan keaslian sementara nominal pertanyaan terpisah, penolak pelunasan kedua adalah `state='paid'`) sudah dibayar mahal sekali dan tidak boleh ditulis ulang setengahnya. | `server/shop/cash-order.ts`; migrasi `0058`; test `SHOP-1` |
| Barang toko boleh punya **dua harga**, dan harga TD-nya selalu bernilai lebih besar daripada harga QRIS-nya | TD adalah liabilitas: satu credit yang tidak dibelanjakan pada akhirnya ditarik jadi Rupiah. Barang yang lebih murah ditebus pakai TD daripada dibayar tunai membalik arah seluruh rak — toko berhenti menyerap saldo dan mulai menggantikan pemasukan tunai, dan yang paling rugi justru user yang membayar pakai uang sungguhan. Aturannya sudah ada untuk premium sejak migrasi `0055`; migrasi `0058` menggeneralisasikannya ke setiap barang berharga ganda. | `validateEconomyConfig`; `domain/store/store.ts`; test `TOKO-5` |
| Dua barang berdiri di ujung berlawanan: premium **hanya TD** di rak, kosmetik **hanya QRIS** | Premium punya lembar tunainya sendiri dengan lima paket, dan tombol Rupiah kedua di rak cuma menjual paket termahal per bulan kepada orang yang tidak melihat pembandingnya. Kosmetik kebalikannya: ia satu-satunya barang yang tidak menyentuh ekonomi sama sekali, jadi menjualnya lewat TD menukar liabilitas dengan sesuatu yang seharusnya jadi pemasukan bersih — dijual tunai ia margin utuh, dijual pakai saldo ia cuma diskon. Migrasi `0058` sempat menyemai harga TD untuk kosmetik; `0059` mencabutnya sebelum raknya dipakai. | `storeCatalog`; migrasi `0059`; test `TOKO-4`, `TOKO-5` |
| **Pass Gaspol** membayar ongkos masuk, bukan hadiahnya | Argumennya sama persis dengan tiket iklan di migrasi `0024`: yang dibeli ongkos masuk, jadi yang naik cuma kecepatan user menghabiskan plafonnya sendiri — kolam reward tetap yang mematok berapa yang bisa keluar per hari. Soal yang dibayar pass sengaja TIDAK menulis `energy_spent_at`: jejak ongkos yang telanjur terisi membuat `refundEntry` mencetak energi dari udara untuk soal yang gratis. | `startChallenge`; migrasi `0058`; test `TASK-GASPOL` |
| **Tarik Sekarang** memakai penanda waktu, bukan jatah yang dicacah | `readEligibility` melepas jeda hanya selama `withdrawal_cooldown_waived_at` lebih baru daripada pengajuan terakhir. Bentuk itu yang membuatnya habis sendiri — begitu pengajuan berikutnya masuk, `requested_at` melewatinya dan gerbangnya menutup lagi, tanpa satu pun langkah konsumsi terpisah yang bisa gagal di tengah dan menyisakan jatah hantu. Ia juga tidak menambah satu credit pun: yang berubah KAPAN saldo yang sudah ada boleh keluar, dan yang naik biaya transfer per rupiah — itu yang harus ditutup harganya. | `readEligibility`; migrasi `0058`; test `WD-13` |
| Kosmetik satu-satunya barang yang **dimiliki**, dan panggungnya papan peringkat | Ia tidak menyentuh ekonomi sama sekali — tidak menambah energi, tidak mempercepat apa pun, tidak menggeser satu credit pun — jadi marginnya utuh. Yang membuatnya laku bukan barangnya melainkan tempat ia terlihat: papan peringkat sudah memajang nama dan foto ke seluruh user, dan kosmetik tanpa panggung tidak pernah terjual. Kepemilikan dipisah dari pemasangan supaya yang dibeli tidak hilang saat diganti. `cosmetic_key` sengaja tidak dibatasi check di database: katalognya di kode dan setiap jalur tulis melewati `isCosmeticKey`, jadi check kedua cuma menyalin daftar yang sama ke tempat yang menuntut migrasi tiap satu bingkai ditambahkan. | `domain/store/cosmetics.ts`; migrasi `0058`; test `TOKO-4` |
| Rak kosmetik yang ditutup admin tetap **menyerahkan** pesanan yang sudah dibayar | `storeCatalog()` menghormati `storeCosmeticsEnabled`, `fulfilmentCatalog()` tidak. Tanpa pemisahan itu, saklar tampilan berubah jadi saklar yang menelan pembayaran orang: QR yang terbit semenit sebelum saklarnya dimatikan tidak punya barang untuk diserahkan lagi. | `fulfilmentCatalog`; test `TOKO-5` |

## Arena

Arena adalah satu-satunya fitur di app ini yang hadiahnya bisa **menaikkan plafon
payout**, bukan cuma mempercepat user mencapainya. Karena itu ia punya bagian
sendiri: hampir setiap keputusan di dalamnya adalah pertukaran biaya yang diambil
sadar, bukan pilihan bentuk.

| Yang terlihat janggal | Kenapa memang begitu | Ditegakkan di |
| --- | --- | --- |
| Hadiahnya energi atau isi stok, **tidak pernah credit** | Credit adalah liabilitas langsung: ia masuk saldo dan bisa ditarik jadi Rupiah tanpa melewati kolam maupun `daily_quotas`. Energi berbiaya nol — kolam tetap mematok berapa yang bisa dibayar, jadi energi cuma menambah task, dan task menambah impresi iklan. Isi stok berbiaya nyata karena ia menaikkan plafon itu sendiri, dan itu justru satu-satunya hadiah yang membuka layar "stok habis". Alasan yang sama sudah ditulis untuk misi harian di `domain/progression/missions.ts`. | `arcadePrizeTable`; test `ARCADE-DB-3 … mengisi stok reward, bukan saldo` |
| Arena **sudah menyala** di produksi, dan itu menggeser plafon penghasilan per akun | Baris ini dulu menyatakan `arcadeEnabled` bawaannya 0 dengan alasan "menyalakannya keputusan ekonomi, bukan efek samping deploy". Keputusan itu sudah diambil: `DEFAULT_ECONOMY_CONFIG` menyetel 1, snapshot `HISTORIC` ikut 1, dan migrasi `0039` menyalakannya di baris produksi. Yang perlu diingat pembaca berikutnya adalah akibatnya, bukan bawaannya: Arena mengisi ulang kolam yang sudah habis, jadi ia menambah kapasitas **penghasilan harian** meski tidak menambah kapasitas tampungan — 3 main x 5 credit = 15 credit/hari pada kasus terbaik, ekspektasi ±3,75 credit/hari pada bobot bawaan. Harga premium di migrasi `0027` dihitung di atas premis "kolam mengisi 30 credit/hari, jadi Rp90.000/bulan sudah jadi langit-langit penghasilan satu akun", dan premis itu **sudah tidak berlaku**. Meninjau ulang harganya adalah keputusan terbuka. | `DEFAULT_ECONOMY_CONFIG`; migrasi `0039`, `0027`; test `ARCADE-1`, `ECONCFG-ARCADE-DEFAULT` |
| Hasil ronde Cocokkan Kartu **dipercaya apa adanya** dari klien | Persis alasan klaim iklan: yang menahan penyalahgunaan bukan bukti bahwa rondenya betul dimainkan, melainkan jatah main harian, jeda, dan pass iklan yang sudah terbakar di pembukaan. User yang selalu mengaku menang paling banter mendapat sebanyak jatah hariannya — angka yang sudah disetujui di panel dan sudah dibayar impresi iklannya. Memverifikasi papan berarti menyimpan susunan kartu dan seluruh urutan langkah di server demi menutup celah yang plafonnya sudah tutup. Undian hadiahnya sendiri **tidak** dipercayakan ke klien. | `settleArcadePlay` |
| Hadiah yang tidak muat **dicoret dari undian**, bukan dibayar sebagian | `applyEnergyGrant` memotong di `maxEnergy()` dan `applyRewardPoolRefund` menjepit di kapasitas, jadi hadiah yang lebih besar dari sisa ruang hilang tanpa jejak sementara barisnya terlanjur mencatat angka penuh. `claimMission` menolak di depan karena alasan yang sama; di sini undiannya yang menyempit, jadi user tetap dapat sesuatu selama masih ada satu hadiah yang muat. | `eligiblePrizes`; test `ARCADE-3`, `ARCADE-DB-4` |
| Stok dan energi sama-sama penuh membuat Arena **menolak sebelum iklannya tayang** | Kalau tidak ada satu pun hadiah yang muat, rondenya sudah pasti zonk. Menyuruh orang menonton iklan demi hasil yang sudah pasti kosong adalah cara tercepat membuat fitur ini dibenci, dan penolakannya harus berdiri sebelum `consumeAdPass`, bukan sesudah. | `hasWinnablePrize`, `arcadeOpenRefusal`; test `ARCADE-DB-4 … passnya masih utuh` |
| Pass iklan dipotong saat **membuka** ronde, bukan saat menyetel hasilnya | Kalau ongkosnya baru ditagih saat menyetel, ronde yang kalah menjadi gratis dan satu tayangan iklan bisa dipakai mencoba berkali-kali sampai menang. Pemisahan yang sama dipakai tiket iklan terhadap `startChallenge`. | `openArcadePlay`; test `ARCADE-DB-1` |
| Jatah harian dihitung dari ronde yang **dibuka**, bukan yang selesai | Ongkosnya sudah dibayar di pembukaan. Menghitung yang selesai saja membuat ronde yang ditinggal menjadi gratis, dan itu jalan termurah untuk menguras jatah tanpa pernah menghabiskannya. Bentuknya sengaja berbeda dari jatah tayangan iklan, yang justru menghitung tiket yang **diklaim** — di sana yang dibatasi hadiah, di sini yang dibatasi biaya. | `STATE_SQL` di `server/arcade/arcade.ts` |
| Ronde yang ditinggal mengembalikan pass iklannya, tapi **tidak** mengembalikan jatah | Iklannya sudah benar-benar ditonton, jadi passnya kembali. Jatahnya tidak, karena barisnya tetap terhitung — tanpa itu, meninggalkan ronde berulang kali menjadi cara menonton iklan tanpa batas. `restoreAdPass` sendiri menolak kalau user sudah memegang pass lain yang siap, jadi stok pass tidak bisa ditumpuk lewat jalur ini. | `expireStalePlays` |
| Ketiga kotak dibuka, termasuk dua yang tidak dipilih | Server memang mengundi tiga hadiah terpisah dan pilihan user memilih salah satunya, jadi memperlihatkan isi kotak lain bukan karangan layar. Undian tunggal yang "dipindahkan" ke kotak yang diketuk akan membuat dua kotak sisanya harus dikarang, dan itu berbohong. | `drawBoxes` di `server/arcade/arcade.ts` |
| Arena **tidak** menambah slot nav pill | Barisnya dipatok lima dan geometri indikatornya dihitung dari jumlah itu (`--nav-active-index`). Pintu masuknya kartu di Beranda, sama seperti Riwayat dan Statistik yang juga view penuh tanpa slot nav. | `NAV_SLOTS` di `navigation/nav-pill.tsx`; `ArcadeCard` |
| Kartu Arena di Beranda tidak melanggar keputusan "stok habis tetap menunggu" | Yang ditolak keputusan di `ActiveTask` adalah aksi yang berpura-pura mempercepat stok. Kartu ini menawarkan **tujuan lain** yang hadiahnya betul-betul mengisi stok, jadi bedanya ada di data, bukan cuma di kata-kata. | komentar di `features/arcade/arcade-card.tsx` |
| Ronde Arena yang ditinggal mengembalikan passnya dari **dua arah**, bukan satu | Ronde basi bisa ditutup oleh sapuan saat Arena dibuka lagi, atau oleh penyetelan yang datang terlambat dari app yang sempat di-background. Keduanya wajib memanggil `restoreAdPass`: begitu barisnya keluar dari state `open`, sapuan berikutnya tidak akan pernah bisa menyusul, jadi jalur yang lupa mengembalikan menghanguskan iklan yang sudah benar-benar ditonton — permanen, tanpa jejak selain baris `arcade_plays` yang tidak berhadiah. Jatah hariannya tetap tidak dikembalikan, dan pesan penolakannya harus menyebut keduanya apa adanya. | `expireStalePlays` dan cabang kedaluwarsa `settleArcadePlay`; test `ARCADE-DB-7` |
| Arena ikut tertutup saat tombol mati iklan menyala | Ongkos masuknya satu pass iklan, dan `adsMaxViewsPerDay = 0` membuat pass itu tidak akan pernah bisa dibuat. Arena yang tetap "menyala" di situ hanya menawarkan tombol yang dijawab "iklan lagi tidak tersedia" — fitur mati dengan pintu masuk yang masih terpampang. Ditutup lewat `arcadeEnabled()` supaya seluruh jalur (kartu Beranda, view, server) ikut satu jawaban, memakai layar `ArcadeClosed` yang sudah ada. Ronde yang terlanjur terbuka tetap bisa disetel, karena `settleArcadePlay` sengaja tidak memeriksa saklar ini. | `arcadeEnabled` di `domain/arcade/arcade.ts`; test `ARCADE-1` |

## Angka yang sengaja tetap di kode

Seluruh besaran yang menggeser penghasilan atau pembayaran ada di panel admin —
46 field, dan `domain/economy/economy-config.test.ts` menolak field tanpa metadata
maupun metadata tanpa field. Yang di bawah ini **bukan** kelalaian; semuanya
tetap di kode karena mengubahnya bukan menyetel ekonomi melainkan mengganti
arti data atau melanggar constraint:

| Angka | Kenapa tidak bisa disetel |
| --- | --- |
| `COMMISSION_UNITS_PER_CREDIT = 100` | Satuan, bukan besaran. Kolom `commission_units` dan `pending_units` menyimpan angka dalam satuan ini, jadi mengubahnya akan mengubah arti seluruh baris komisi yang sudah tersimpan. |
| `STAR_MAX = 3` dan tipe `StarCount` | Terikat constraint `task_completions.stars between 1 and 3`. |
| 5 tingkat rank | Terikat daftar nama rank di `domain/progression/progression.ts`. Ambang tiap tingkatnya sendiri bisa disetel. |
| `FLOOR_MS` dan `SWEEP_THRESHOLDS` di `server/task/fraud.ts` | Ambang sinyal anti-fraud. Ia hanya mencatat, tidak pernah mengubah reward maupun menolak pembayaran. Angkanya dikalibrasi dari sebaran nyata produksi, bukan ditebak: `identicalTimingMaxSpreadMs` 600 berdiri di bawah lantai manusia paling konsisten yang terukur (1.877ms), dan `noWrongMaxErrorRatio` 1% berdiri di bawah rata-rata populasi (~6,5%). |
| Batas retensi, ukuran halaman, rate limit, umur sesi | Operasional, bukan ekonomi. |
| `referralBurstLookbackMinutes` (1.500) jauh lebih panjang dari `referralBurstWindowMinutes` (10) | Dua angka berbeda karena menjawab dua hal berbeda: yang pertama **seberapa jauh ke belakang disapu**, yang kedua **serapat apa yang dicari**. Versi pertama menyatukan keduanya jadi satu jendela 10 menit, padahal cron waktu itu jalan sekali sejam (`railway.cron.json`) — jadi 50 dari 60 menit tidak pernah terlihat dan detektornya menulis nol baris selama dua belas hari. Rentang sapuan harus selalu melampaui periode cron: sekarang 25 jam, menutupi workflow harian di `.github/workflows/maintenance.yml` dengan margin satu jam. Kerapatannya tetap sempit supaya artinya tidak tumpul. Dijaga test `FRAUD-4`, yang gagal kalau jadwal cron diubah tanpa menaikkan rentangnya. |

Satu batas datang dari database, bukan dari selera: `maxAttemptsPerTask`
maksimal 5, karena `challenges.attempts` punya `check (attempts between 0 and
5)`. Menaikkan batasnya berarti satu migrasi lebih dulu.

## Pesan bot

| Yang terlihat janggal | Kenapa memang begitu | Ditegakkan di |
| --- | --- | --- |
| Pesan ajakan dikirim dari cron tiap jam, bukan dari alur yang memicunya | Mengirim HTTP ke Telegram di dalam transaksi yang memegang `for update` pada baris `users` menahan kunci selama panggilan jaringan — di jalur uang itu tidak boleh. Cron juga yang membuat "komisi masuk" jadi satu ringkasan harian, bukan belasan pesan per hari. | `server/messaging/engagement.ts`; `scripts/maintenance.ts` |
| Penanda `bot_notifications` ditulis **sebelum** pesannya dikirim | `bot_notifications_once` cuma menjamin sesuatu kalau barisnya sudah commit sebelum panggilan Telegram. Kirim yang gagal menghapus penandanya lagi, jadi cron berikutnya boleh mencoba ulang — telat sejam lebih baik daripada hilang diam-diam, dan dua-duanya lebih baik daripada satu pesan berangkat dua kali. | `deliver`; test `ENG-9` |
| Satu user paling banyak menerima **satu** pesan per putaran cron | Semua syarat bisa terpenuhi berbarengan (energi penuh + stok penuh + streak sore). Mengirim semuanya membuat bot terbaca sebagai spam dan mengundang laporan. Urutannya: penarikan → rank → streak → misi → komisi → referral → musim → winback → stok → toko → energi → ajakan harian. Karena cron-nya jalan sekali sehari, urutan itu menentukan segalanya — jenis yang kalah urutan bukan "muncul nanti", melainkan tidak pernah muncul untuk user itu hari itu. | `pickMessage` |
| Setiap pesan ditutup **ajakan main yang menyebut angka**, dan tombolnya menyebut aksinya | Bentuk lamanya berhenti sebagai laporan keadaan: "komisi kamu masuk, tanpa ngapa-ngapain" dengan tombol "Buka app". Pesan yang cuma menyatakan keadaan membuat orang berhenti, dan satu-satunya jatah kirim hari itu habis tanpa memindahkan siapa pun. Perkiraan hasilnya diambil dari reward Sedang 2★ dan DIJEPIT sisa stok reward — janji besar di atas stok yang tinggal sedikit dibantah aplikasi sendiri begitu user membukanya. | `invite`; test `ENG-12` |
| `daily_invite` adalah jaring terakhir, bukan pesan cadangan | Tanpa jenis ini, user yang hari ini belum menyentuh soal — tapi energinya belum penuh, stoknya belum penuh, streak-nya belum dua hari — tidak menerima satu pun ajakan main, padahal ia persis orang yang paling mudah diajak balik. | `pickMessage`; test `ENG-12` |
| Ajakan belanja (`store_idle`) memakai kunci dedup **mingguan** | Ajakan belanja yang datang tiap hari berhenti jadi ajakan dan mulai jadi gangguan. Sasarannya juga sempit dengan sengaja: saldo yang cukup untuk membeli sesuatu di rak hari itu, penarikan yang belum kebuka, dan tidak ada kabar lain yang lebih pantas. | `storeNudgeFits`; test `ENG-12` |
| `mission_ready` cuma menyebut misi yang **tinggal sedikit**, dan cuma kalau misinya keluar hari itu | Misi harian diundi (migrasi `0048`), jadi menyebut misi yang tidak keluar adalah pesan yang salah — user yang membukanya tidak menemukan apa pun yang cocok. Ambang "tinggal dua soal" ada karena empat soal lagi bukan ajakan, cuma laporan. | `dailyTasksMissionLeft`; test `ENG-12` |
| `rank_up` menengok jumlah task 24 jam lalu, bukan cuma rank sekarang | Tanpa pembanding itu, putaran cron pertama setelah deploy mengucapkan selamat naik rank ke setiap user yang sudah lama di rank-nya. `dedupe_key`-nya tingkat rank, jadi pesannya tetap sekali per tingkat. | `completed_count_before`; test `ENG-5` |
| Streak di `engagement.ts` dihitung sampai **kemarin**, bukan sampai hari ini seperti `STREAK_EXPRESSION` | Pesannya justru untuk user yang belum menyentuh task hari ini, jadi hari ini tidak boleh ikut menambah angkanya. Batas harinya tetap WIB dan bentuk querinya tetap sama. | `STREAK_SQL`; test `ENG-3` |
| `/stop` tidak mematikan notifikasi penarikan | Yang dimatikan promosi, bukan kabar uang. User yang mengajukan penarikan berhak tahu hasilnya. | migrasi `0026`; `server/messaging/notify.ts` |
| `server/messaging/engagement.ts` memakai impor relatif ber-`.ts`, bukan alias `@/` | Ia dimuat cron lewat `node --experimental-strip-types`, dan Node tidak mengerti alias `@/...` dari tsconfig. Impor **tipe** boleh tetap beralias karena dihapus saat strip. Alasan yang sama membuat `payout-rules.ts` dipisah dari `payout.ts`: yang terakhir menyeret `next/headers` lewat `./session`. | `server/messaging/engagement.ts`; `server/payout/payout-rules.ts` |

## Nama "kolam" tinggal di kode, tidak di layar

`reward_pool`, `rewardPoolCapacity`, dan seluruh nama fungsi tetap memakai "pool"/"kolam".
Yang dilihat user memakai **"stok reward"** — dikonfirmasi pemilik repo, karena "kolam"
terbaca seperti kolam renang. Jangan menyamakan keduanya dengan rename massal: nama kode
sudah dipakai di migrasi, kolom DB, dan test. Label panel admin masih memakai "kolam" dan
itu belum diputuskan; admin panel dibaca pemilik repo, bukan user.

Sebelumnya pill kolam di header berlabel **"Limit harian"** — sisa dari plafon harian yang
sudah diganti migrasi `0023`. Sekarang "Stok reward", karena tidak ada lagi yang reset
tengah malam.

## Batas hari

Seluruh konsep "hari" memakai WIB dan harus sama persis dengan
`(now() at time zone 'Asia/Jakarta')::date` di SQL — streak, batas task harian,
plafon komisi referral, `daily_quotas.quota_date`, dan label waktu di UI.
Penghasilan task sendiri **tidak lagi** ikut batas hari: kolam reward memakai
jam acuan regen, bukan tanggal WIB. `formatHistoryTime` mengunci
zona waktunya sendiri; ia sengaja **tidak** memakai zona perangkat, karena user
Indonesia tersebar di tiga zona. Lihat test `formatHistoryTime` di
`shared/lib/format.test.ts` yang dijalankan di zona non-WIB.

## Penamaan

Nama rank memakai bahasa Inggris — Apprentice, Artisan, Expert, Virtuoso,
Luminary — dan itu **disengaja**, dikonfirmasi pemilik repo. Ini pengecualian
sadar terhadap aturan keras #7 di `CLAUDE.md` yang mewajibkan bahasa UI
Indonesia; pengecualiannya berhenti di nama rank saja, seluruh teks lain tetap
Indonesia. Jangan diterjemahkan balik tanpa diminta.

Panel pill profil sengaja hanya memuat empat baris ringkasan — identitas, task,
kualitas, saldo — lalu menutup dengan tombol "Lihat statistik", **bukan** seluruh
angka view Statistik. Tinggi panel island dianimasikan dari `scrollHeight`; isi
sepanjang view Statistik akan melewati tinggi layar 384×639 dan menuntut scroll
di dalam header, bentuk yang tidak ada di tempat lain di app ini. Streak juga
sengaja tidak diulang karena sudah jadi satu region di panel rank.

Pill profil **hilang di view task** (`captcha`). Di lebar 384px tiga pill
menyisakan ruang terlalu tipis, dan saat mengerjakan task yang relevan tinggal
rank dan kesulitan. Karena `.brand-band-row` di-center, pill yang hilang membuat
klaster tier+sulit recenter sendiri, dan `useIslandGeometry` mengukur ulang lewat
observer barisnya. `ProgressionBadges` mereset `openPanel` saat pill sumbernya
berhenti dirender supaya panel tidak tertinggal terbuka pada komponen yang sudah
di-unmount.

Glyph rank tingkat satu sengaja **bukan** segitiga. Bentuk sebelumnya —
segitiga dengan titik di tengah — terbaca sebagai piramida bermata, dan pada
14px jatuhnya mirip ikon peringatan. Diganti chevron bergaris.

| Panel admin punya **root layout dan CSP sendiri** | Dulu ada satu root layout untuk seluruh aplikasi, jadi SDK iklan Monetag ikut dieksekusi di `/admin/*` — same-origin dengan cookie sesi admin, sehingga satu skrip pihak ketiga yang bermusuhan bisa memanggil `/api/admin/*` dan lolos setiap penjagaan yang ada (semuanya dirancang untuk permintaan lintas-situs). Sekarang `app/(miniapp)/` dan `app/(admin)/` adalah dua root layout terpisah, dan `proxy.ts` mencabangkan CSP-nya. Rinciannya di `docs/adr/0005-panel-admin-di-root-layout-sendiri.md`. | `proxy.ts`; test `CSP-1`, `CSP-3` |

## Suara aplikasi

Yang memakai app ini bapak-bapak dan emak-emak yang membuka Telegram buat nambah
penghasilan — bukan orang yang pernah membaca dokumentasi HTTP. Setiap kalimat di layar
ditulis untuk mereka, dan itu bukan soal selera: pesan yang tidak dimengerti sama saja
dengan pesan yang tidak ada, dan pada layar yang menyangkut uang itu berakhir jadi
tiket support.

**Aturannya:**

1. **Kabar dulu, penjelasan belakangan.** "Cair! Dana udah kami kirim", bukan "Penarikan
   Anda telah berhasil diproses". Kalau kalimat pertama sudah menjawab "jadi gimana?",
   sisanya boleh dilewat pembaca.
2. **Selalu ada langkah berikutnya.** Pesan yang cuma menyatakan keadaan bikin orang
   berhenti. "Soalnya udah nggak ada. **Ambil soal baru ya.**"
3. **Satu kalimat kalau bisa, dua kalau perlu.** Toast lewat dalam beberapa detik.
4. **Sebut yang ditakutkan orang, lalu tenangkan.** Yang dikhawatirkan waktu iklan gagal
   cuma satu: jatahnya hangus atau tidak. Jadi tiap pesan kegagalan iklan menutup dengan
   "jatah kamu aman".
5. **Tidak ada kode teknis di layar.** `Kode: AD-TIMEOUT` pernah lolos ke produksi sebagai
   sisa sesi debugging; bagi user itu terbaca seperti aplikasinya rusak parah, dan tidak
   satu pun dari mereka akan melaporkannya. Pelacakan kegagalan tempatnya di log.
6. **Nama internal ≠ nama di layar.** Kode menyebut `task`, `challenge`, `pool`, `pass`;
   layar menyebut **soal**, **stok**, **tiket**. Satu benda satu nama di layar — dulu
   "task" dan "soal" dipakai bergantian untuk hal yang sama, bahkan dalam satu kalimat
   ("Task-nya belum dimulai. Ambil soal baru ya."), dan pembaca yang tidak akrab teknologi
   mengira itu dua hal berbeda. Dipilih **soal** karena Indonesia, karena sudah dominan di
   pesan error, dan karena ia jelas berbeda dari **misi**.

| Jangan | Pakai |
| --- | --- |
| cooldown | jeda |
| clipboard | kesalin / salin teks |
| task | soal |
| sesi | sebut langkahnya: "Buka Tugas Duit dari Telegram ya." |
| Anda, silakan, harap, mohon | kamu, kalimat perintah langsung |
| tidak valid, Body tidak valid | nggak kebaca |
| Kode: AD-XXXX | (buang) |

Panel admin **di luar aturan ini**: ia dipakai satu orang yang tahu persis apa itu
`rate_limits`, dan di sana ketepatan istilah lebih berharga daripada keakraban.

Ditegakkan `COPY-1` di `tests/copy.test.ts`, yang menyapu seluruh permukaan peserta dan
gagal kalau satu kata terlarang muncul lagi. Yang tidak bisa diuji mesin — panjang
kalimat, urutan kabar lalu penjelasan — tidak dicoba diuji.

## Data & skema

| Yang terlihat janggal | Kenapa memang begitu |
| --- | --- |
| `referral_commissions.settled_ledger_id` dan `withdrawals.hold_ledger_id` ditulis tapi tidak pernah dibaca aplikasi | Keduanya jejak audit uang: menghubungkan satu baris komisi/penarikan ke baris ledger yang menyelesaikannya. Dipakai saat rekonsiliasi, bukan saat melayani request. |
| `users.profile_overridden_at` hanya penanda, tidak pernah ditampilkan | Begitu admin mengoreksi nama atau username lewat panel, upsert login berhenti menimpa dua kolom itu dari `initData` untuk baris tersebut saja. Tanpa penandanya, koreksi admin hilang diam-diam pada login berikutnya. `photo_url` sengaja tidak ikut dikunci. |
| `sessions.user_agent` ditulis tapi tidak dibaca | Jejak forensik sesi. |
| `credit_ledger` tidak punya jalur update maupun delete | Append-only, ditegakkan trigger — termasuk `truncate`. | 
| Migrasi bernomor lompat (`0017` lalu `0020`) | Migrasi bersifat historis: hanya boleh ditambah, tidak pernah diedit atau dinomori ulang. |

Kolom yang **ditulis tapi tidak pernah dibaca oleh siapa pun** diperlakukan
sebaliknya — dibuang, karena menyimpan atribut akun orang tanpa pemakai berarti
menanggung datanya tanpa imbalan: `is_premium` (migrasi `0007`), lalu
`last_name` dan `language_code` (migrasi `0021`). Ketiganya datang dari
`initData` setiap login, jadi menghidupkannya kembali cukup satu `add column`
dan tidak ada data historis yang hilang.

## Kode yang terlihat mati padahal tidak

| Yang terlihat janggal | Kenapa memang begitu |
| --- | --- |
| `LEADERBOARD_ENABLED = false` sementara `features/leaderboard/*` dan `/api/leaderboard` lengkap | Fitur sengaja dimatikan, bukan ditinggalkan. Menyalakannya cukup mengubah satu konstanta. |
| `shadcn` devDependency tidak pernah diimpor dari `.ts` | Dipakai lewat `@import 'shadcn/tailwind.css'` di `app/globals.css`. |
| `export const contentType` di `app/apple-icon.tsx` tanpa pemanggil | Konvensi Next.js; frameworknya yang membacanya. |
| Ikon SVG disalin manual ke `shared/components/glyph.tsx` | Disengaja, tidak memakai library ikon. |

## Paritas yang dijaga uji, bukan kebiasaan

- `PAYOUT_CHANNELS` di `domain/economy/withdrawal.ts` harus sama persis dengan
  constraint `withdrawals_known_channel`. Selisih di antara keduanya berarti
  penarikan lolos TypeScript lalu ditolak database dengan `23514` — 500 di jalur
  uang. Dijaga `server/payout/payout.test.ts` (`WD-6`), yang mencoba setiap channel dari
  daftar itu ke database sungguhan.
- Setiap key di `EconomyConfig` harus punya pembaca. `maxTasksPerDay` pernah
  tidak punya, dan panel admin diam-diam tidak berpengaruh. Dijaga
  `ECON-4` di `server/economy-guards.integration.test.ts`.
