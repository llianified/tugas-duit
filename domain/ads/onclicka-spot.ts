/** Spot ID rewarded OnClicka (AD Code 461406, spot "Tugas Duit — TMA Rewarded video #1").
 * Yang dipakai `initCdTma({ id })` adalah SPOT ID, bukan AD Code ID; menukar keduanya
 * membuat init selalu reject dan rewarded tidak pernah tersedia. */
export const ONCLICKA_SPOT_ID = '6145580'
