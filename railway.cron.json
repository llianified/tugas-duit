{
  "$schema": "https://railway.app/railway.schema.json",
  "build": { "builder": "NIXPACKS", "buildCommand": "echo 'cron service: tanpa build, lihat scripts/maintenance.ts'" },
  "deploy": {
    "startCommand": "pnpm db:cleanup",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER",
    "numReplicas": 1
  }
}
