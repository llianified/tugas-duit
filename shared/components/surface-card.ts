/** Permukaan kartu sekunder: kotak `--muted` yang dipakai kartu-kartu di bawah hero. Sebuah konstanta, bukan komponen. Ketiga pemakainya membutuhkan elemen yang berbeda — `<section aria-label>` untuk kartu bonus, `<section>` yang permukaannya dilepas saat daftar misi menjadi isi view, dan `<div aria-hidden>` untuk kerangkanya — jadi sebuah komponen pembungkus hanya akan menambah prop `as` dan `surface` untuk mengembalikan keluwesan yang baru saja dihilangkannya. Yang benar-benar terduplikasi cuma nilai kelasnya: sebelum ini tertulis tiga kali, dan padding kerangka pernah lepas dari padding kartu justru karena begitu. */
export const SURFACE_CARD_CLASS = 'rounded-lg bg-muted/60 p-[var(--surface-p)] ring-border'
